#include <bits/stdc++.h>
using namespace std;
const int N = 1e5 + 10;
int a[N];

void merge(int l, int r, int mid)
{
    int l_size = mid - l + 1;
    int L[l_size + 1];
    int r_size = r - mid;
    int R[r_size];
    for (int i = 0; i < l_size; i++)
    {
        L[i] = a[i + 1];
    }
}
int main()
{
    int n;
    cin >> n;
    int a[n];
    for (int i = 0; i < n; i++)
    {
    }
}
